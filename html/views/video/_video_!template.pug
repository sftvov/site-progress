section.section
  .container
    .col3.n1o1
      h2.t1d !{video.title}
      a._popup-link.play(href="#video" data-platform="vk" data-video=`[47184958,${video.id}]`)
        +picture(996, 'img/contacts/', video.img, 'png', 'w','', true)